apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend-deployment
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    nodePort: 30304
